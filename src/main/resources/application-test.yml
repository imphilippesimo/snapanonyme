spring:
  application:
    name: snapanonyme
  ## MULTIPART (MultipartProperties)
  # Enable multipart uploads
  servlet:
    multipart:
      enabled: true
      # Threshold after which files are written to disk.
      file-size-threshold: 2KB
      # Max file size.
      max-file-size: 200MB
      # Max Request Size
      max-request-size: 215MB

    #  datasource:

    #    url: jdbc:mysql://localhost:3306/snapanonyme?createDatabaseIfNotExist=true
    #    username: snapanonyme
    #    password: Banzai85$

    datasource:
      url: jdbc:h2:mem:testdb
      driverClassName: org.h2.Driver
      username: sa
      password: password

    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      show-sql: true
      generate-ddl: true
      hibernate:
        ddl-auto: update

  #  datasource:
  #    url: jdbc:mysql://${SNAPANONYM_DB_HOST}:${SNAPANONYM_DB_PORT}/${SNAPANONYM_DB_NAME}?createDatabaseIfNotExist=true
  #    username: ${SNAPANONYM_DB_USER}
  #    password: ${SNAPANONYM_DB_PASSWORD}
  #
  #  jpa:
  #    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  #    show-sql: true
  #    generate-ddl: true
  #    hibernate:
  #      ddl-auto: update

  ## Hibernate Logging
  logging:
    level:
      org.hibernate.SQL: DEBUG





server:
  port: 9000